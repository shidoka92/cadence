/**
 * Types de la base — à GÉNÉRER après création du schéma :
 *   supabase gen types typescript --project-id <id> > packages/db/src/database.types.ts
 */
export type Database = Record<string, unknown>;
