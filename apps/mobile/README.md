# Cadence — App élève (Expo / React Native)

**Phase 2.** Squelette à initialiser quand le coach web est en ligne :

```bash
cd apps/mobile
npx create-expo-app@latest . --template
# puis : NativeWind + @cadence/tokens (mêmes couleurs/typo qu'Arena web)
#         @cadence/types + @cadence/db (modèle + Supabase partagés)
```
Le design system Arena se partage via NativeWind (Tailwind en RN) : mêmes tokens, deux surfaces.
